apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-main-config # Renamed for clarity
  namespace: homepage
  labels:
    app.kubernetes.io/name: homepage
data:
  # From your settings.yaml content
  settings.yaml: |
    title: Lab1830 Dashboard

  # From your bookmarks.yaml content
  bookmarks.yaml: |
   
  # From official example data structure
  services.yaml: |
   
  widgets.yaml: |

  kubernetes.yaml: |
    mode: cluster # For in-cluster Kubernetes widget API access

  docker.yaml: "" # Empty as per example, if not using Docker widget

  custom.css: |
    /* Add your custom CSS here later if needed */

  custom.js: |
    // Add your custom JS here later if needed